import React from 'react';

const ManageEventDetailPage = ({ params }: { params: { id: string } }) => {
  console.log(params.id);

  return <div>ManageEventDetailPage</div>;
};

export default ManageEventDetailPage;
